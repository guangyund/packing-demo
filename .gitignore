# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── 环境变量（含 API Key、DB 密码，绝不入库） ──────────────
.env
.env.*

# ── SSH 密钥 ────────────────────────────────────────────
*.pem
*.key
*.p12

# ── 运行时数据 ───────────────────────────────────────────
results/
feedback_log.jsonl
feedback_store.json
packing_result.png

# ── 日志 & 构建产物 ──────────────────────────────────────
*.log
*.zip
*.tar.gz

# ── 向量数据库（本地索引，服务器单独重建） ──────────────────
chroma_db/

# ── Claude Code 工作目录 ─────────────────────────────────
.claude/

# ── 临时 / 无意义文件 ────────────────────────────────────
=*
*.tmp
*.bak

# ── IDE ──────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.sw?
