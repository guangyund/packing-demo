# 三维装箱系统 — 兜底算法逻辑文档

---

## 一、整体流程

```
用户发起计算请求
       │
       ▼
  AI 模式开启？
  ├── 是 → 调用 Claude API（工具：recommend_and_compare）
  │         │
  │         ├── API 正常响应 + 工具调用成功 → ai_used=True，返回结果
  │         │
  │         └── API 失败 / max_tokens / 超时 → 触发兜底
  │
  └── 否 → 直接走兜底逻辑
             │
             ▼
       本地算法（_do_recommend_and_compare）
       ai_used=False，标注"本地模式"
```

---

## 二、兜底触发条件

以下任一情况触发兜底：

| 触发原因 | 说明 |
|---------|------|
| `max_tokens` | 货物数量多，工具调用 JSON 超过 4096 token |
| API 异常 | 网络超时、限流、IP 被封等 |
| 手动关闭 AI 模式 | 前端关闭 AI 开关 |
| Claude 未返回工具调用 | 10 轮循环内未产出有效装箱结果 |

触发后执行：
```python
_, full_compare, rec_result = _do_recommend_and_compare(items, bins=bins)
```

---

## 三、兜底核心逻辑：`_do_recommend_and_compare`

### 3.1 第一步：包材候选预筛选

函数：`_prefilter_catalog_bins(items, max_results=150)`

从 8267 条包材目录中筛选候选箱型，筛选规则：

| 条件 | 说明 |
|------|------|
| 类型必须是硬包材 | 软包材单独处理 |
| 箱型承重 ≥ 货物总重 | 重量可行性 |
| 箱型体积 ≥ 货物总体积 | 体积可行性 |
| 每件货物旋转后能放入箱中 | 三边升序逐一对比 |

结果按体积升序排列，最多取前 150 个候选。

---

### 3.2 第二步：扫描最优现有箱型

对每个候选箱型用 `scan_mode=True`（贪心快速模式，跳过 OR-Tools）打分：

```
全部装完时：score = 10000 - 箱数×10 + 利用率
未全部装完：score = 已装件数×10 + 利用率
```

找到最高分的箱型后，**单独跑一次完整 OR-Tools 装箱**，得到精确坐标用于 3D 展示。

**早返回条件（跳过后续步骤）：**
> 现有箱型已单箱装完 + 利用率 ≥ 80% + 无降档机会 + 无软包材降档

满足则直接返回现有箱型结果。

---

### 3.3 第三步：计算推荐新包材

函数：`_calc_recommended_bin(items)`

算法步骤：
1. 货物按体积从大到小排序
2. 按重量上限（`MAX_BIN_WEIGHT=22kg`）分批
3. 每批按底面积从大到小垂直叠放：
   - L、W 取各件最大值
   - H 逐件累加
4. 加缓冲余量（`BIN_SIZE_BUFFER=0.5cm`），向上取整（精度 `BIN_SIZE_ROUND_TO=0.5cm`）
5. 应用尺寸上限约束：最长边 ≤ 91cm，另外两边 ≤ 63.5cm

推荐包材**必须单箱装完所有货物**，否则视为无效。

**尺寸收紧优化：** 推荐包材单箱装完后，用实际货物 bbox + buffer 缩减虚大维度。

---

### 3.4 第四步：降档边界包材检测

若推荐包材无法降低 FBA 费档级，逐一尝试预设边界包材：

| 边界包材 | 尺寸 | 承重 |
|---------|------|------|
| 大号标准件边界 | 43×34×19 cm | 9.08 kg |
| 小号大件边界 | 92×70×49 cm | 22.68 kg |

只要能单箱装完且档级更低，立即采用该方案。

---

### 3.5 第五步：软包材方案计算

条件：所有货物均标记 `soft_packaging_ok=True` 且总重 ≤ 10kg

软包材尺寸公式：
```
pkg_L = stack_L + stack_H × 0.5 + 0（封口余量）
pkg_W = stack_W + 0.2 × 2（两侧余量）
pkg_H = stack_H
```

---

### 3.6 第六步：Winner 选择

收集所有可行方案，按 **(FBA费档级, 总费用)** 升序排列，取最优：

```python
候选列表 = [
    (rec_tier_rank,      rec_total_fee,      "推荐新包材"),   # 如果单箱装完
    (soft_tier_rank,     soft_total_fee,     "软包材"),       # 如果软包可行
    (existing_tier_rank, existing_total_fee, 现有最优箱型名), # 如果全部放完
]
winner = 排序后第一个
```

---

## 四、装箱核心算法：`calculate_packing`

### 4.1 单箱优先策略

按体积从小到大遍历箱型，对每个箱型：
1. 快速过滤：总重超限 / 总体积超限 / 存在单件放不进去 → 跳过
2. 通过过滤 → 跑完整装箱（贪心 + OR-Tools）
3. 找到利用率 ≥ 80% 的最小箱型即停止

### 4.2 多箱兜底策略

无法单箱时，贪心多箱：每轮从剩余货物中选出最优箱型，重复直到全部装完。

---

## 五、单箱装箱算法：`_pack_bin`

### 5.1 贪心热启动（`_greedy_pack`）

- 货物按体积从大到小排序，大件优先
- 候选放置位置：已放置货物的角点坐标
- 每件货物尝试 6 种旋转方向（L/W/H 全排列）
- 评分标准：位置坐标 (z, y, x) 字典序最小（尽量贴地靠角）
- 支撑率检查：`ENABLE_SUPPORT_CHECK=True` 时，货物底面支撑面积比 ≥ 75% 才接受

多策略搜索：对 6 种旋转方向各跑一次贪心，取装入件数最多的结果。

### 5.2 OR-Tools 精化（CP-SAT 求解器）

- 以贪心结果作为初始 Hint，固定旋转方向，只优化坐标
- 时间限制：`ORTOOLS_TIME_LIMIT=1.5s`，并行线程：`ORTOOLS_NUM_WORKERS=8`
- 超时未找到更优解 → 直接返回贪心结果（保证有解）

### 5.3 重力沉降后处理（`_settle_gravity`）

`ENABLE_GRAVITY_SETTLING=True` 时对 OR-Tools 输出结果做重力模拟：
- 每件货物尝试沉降到最低可落位置
- 落点须满足支撑率 ≥ 75%
- 消除 3D 展示中的浮空现象

---

## 六、FBA 费档级计算

函数：`calc_bin_fee(bin_dims, total_weight_kg, sale_price_usd, product_category)`

**档级判定规则（L ≥ W ≥ H）：**

| 档级 | 尺寸条件 | 重量条件 |
|------|---------|---------|
| 小号标准件 | L≤36, W≤28, H≤1.6 | 实际重≤454g |
| 大号标准件 | L≤43, W≤34, H≤19 | 实际重≤9080g |
| 小号大件 | L≤92, W≤70, H≤49, 围长≤330 | 实际重≤22680g |
| 大号大件 | L≤147, W≤81, H≤81, 围长≤330 | 计费重≤22675g |
| 超大件 | 超出大号大件限制 | — |
| 特大件 | 任一边 > 241cm | — |

计费重 = max(实际重量, 体积重)，体积重 = L×W×H / 5（单位 g）

---

## 七、相关配置参数（config.py）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `MAX_BIN_WEIGHT` | 22 kg | 每箱最大承重 |
| `MAX_BIN_LONG_SIDE` | 91 cm | 推荐包材最长边上限 |
| `MAX_BIN_SHORT_SIDE` | 63.5 cm | 推荐包材另外两边上限 |
| `BIN_SIZE_BUFFER` | 0.5 cm | 推荐尺寸缓冲余量 |
| `BIN_SIZE_ROUND_TO` | 0.5 cm | 尺寸取整精度 |
| `MIN_UTILIZATION_THRESHOLD` | 0.65 | 触发推荐新包材的利用率阈值 |
| `ORTOOLS_TIME_LIMIT` | 1.5 s | OR-Tools 每箱最长计算时间 |
| `ORTOOLS_NUM_WORKERS` | 8 | OR-Tools 并行线程数 |
| `ENABLE_GRAVITY_SETTLING` | True | 是否启用重力沉降 |
| `ENABLE_SUPPORT_CHECK` | True | 是否启用底面支撑率检查 |
| `MIN_ITEM_SUPPORT_RATIO` | 0.75 | 最小支撑面积比 |
| `SOFT_PKG_HEIGHT_FACTOR` | 0.5 | 软包材底部弯折折算比例 |
| `SOFT_PKG_SEAL_BASE` | 0.0 cm | 软包材封口固定留量 |
| `SOFT_PKG_SIDE_MARGIN` | 0.2 cm | 软包材两侧余量 |
| `SOFT_PKG_MAX_WEIGHT` | 10 kg | 软包材重量上限 |
